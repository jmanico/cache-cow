namespace CacheCow.Modules.CatalogInventory.Tests;

// Placeholder so the project compiles before its domain issues land.
public sealed class PlaceholderTests
{
    [Xunit.Fact]
    public void Module_registration_type_exists()
    {
        Xunit.Assert.NotNull(typeof(CacheCow.Modules.CatalogInventory.CatalogInventoryModule));
    }
}
